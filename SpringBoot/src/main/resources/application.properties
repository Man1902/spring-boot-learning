#server.port=9099

# H2 Database config (as a persisted database not In memory)
spring.datasource.name=learning_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.initialize=false
spring.datasource.url=jdbc:h2:~/learning_db;DB_CLOSE_ON_EXIT=FALSE;IFEXISTS=TRUE;DB_CLOSE_DELAY=-1;
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto = none

# Note : for jsp add tomcat-embed-jasper dependency also
#spring.mvc.view.prefix=/WEB-INF/jsp/
#spring.mvc.view.suffix=.jsp

#logging.level.org.springframework=DEBUG

#spring.profiles.active=dev

#Before Spring Boot 2.0
#server.context-path=/SpringBootContext
#After Spring Boot 2.0
#server.servlet.context-path=/SpringBootContext